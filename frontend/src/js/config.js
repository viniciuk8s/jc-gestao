/* config.js — configuração do front (JC Elétrica & Solar).
 * Carregue ANTES de api.js:  supabase.js → config.js → api.js → módulos.
 */
window.JC_CONFIG = {
  // Troque pela URL do seu backend no Render. Ex.: https://jc-backend.onrender.com
  // (sem barra no final)
  API_BASE: "https://jc-gestao.onrender.com",
};
